# 数据目录

首次启动应用时，会在本目录下自动创建 `inventory.db`（SQLite）。

- **开源/分享版本不应包含**任何真实业务数据库文件；仓库中仅保留说明与 `.gitkeep`。
- 部署或克隆后，由程序在本地生成空库并执行轻量级结构补丁（见 `app/database.py`）。
- **物料分类**：开源发布不写入任何预设分类；请在 Web「类别管理」中自行维护后再录入物料。
- **请自行定期备份**生产环境中的数据库与 `uploads/` 目录。
