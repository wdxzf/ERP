# 两人共用、单机部署时，可复制为 .env 后按需修改。
# docker compose 会读取这些变量并传给应用；请勿提交真实值。

# 对外暴露端口；默认 8000
ERP_PORT=8000

# BOM 删除口令；默认兼容旧值 649238，建议改成只有你们两人知道的新值
BOM_DELETE_PASSWORD=649238

# 若你后续自行扩展，也可在这里记录额外变量名：
# TAOBAO_APP_KEY=
# TAOBAO_APP_SECRET=
